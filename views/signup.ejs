<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/css/bootstrap/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: #f8fafc;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .signup-container {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            overflow: hidden;
            max-width: 450px;
            width: 100%;
            border: 1px solid #e2e8f0;
        }
        
        .signup-header {
            background: #0ea5e9;
            color: white;
            padding: 2.5rem 2rem;
            text-align: center;
        }
        
        .signup-header .icon-wrapper {
            margin-bottom: 1rem;
        }
        
        .signup-header i {
            font-size: 2.5rem;
            opacity: 0.9;
        }
        
        .signup-header h2 {
            margin: 0;
            font-weight: 600;
            font-size: 1.5rem;
        }
        
        .signup-header p {
            margin: 0.5rem 0 0 0;
            opacity: 0.8;
            font-size: 0.9rem;
        }
        
        .signup-body {
            padding: 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
            width: 100%;
            max-width: 350px;
        }
        
        .form-label {
            font-weight: 500;
            color: #374151;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
            text-align: left;
            width: 100%;
            display: block;
        }
        
        .form-label i {
            color: #6b7280;
            margin-right: 0.5rem;
        }
        
        .input-group {
            position: relative;
        }
        
        .form-control {
            border: 1px solid #d1d5db;
            border-radius: 8px;
            padding: 0.75rem 0.75rem 0.75rem 2.5rem;
            transition: all 0.2s ease;
            width: 100%;
            font-size: 0.9rem;
            background: #ffffff;
        }
        
        .form-control:focus {
            outline: none;
            border-color: #0ea5e9;
            box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.1);
        }
        
        .input-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: #9ca3af;
            font-size: 0.9rem;
        }
        
        .btn-signup {
            background: #0ea5e9;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 0.75rem 2rem;
            font-weight: 500;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.2s ease;
            width: 100%;
            max-width: 350px;
        }
        
        .btn-signup:hover {
            background: #0284c7;
            transform: translateY(-1px);
        }
        
        .btn-signup:active {
            transform: translateY(0);
        }
        
        .alert {
            background: #fef2f2;
            border: 1px solid #fecaca;
            color: #dc2626;
            padding: 0.75rem;
            border-radius: 8px;
            margin-bottom: 1rem;
            font-size: 0.85rem;
            width: 100%;
            max-width: 350px;
        }
        
        .alert i {
            margin-right: 0.5rem;
        }
        
        .success-alert {
            background: #f0fdf4;
            border: 1px solid #bbf7d0;
            color: #166534;
        }
        
        .links {
            margin-top: 1.5rem;
            text-align: center;
            width: 100%;
        }
        
        .links a {
            color: #0ea5e9;
            text-decoration: none;
            font-size: 0.85rem;
            margin: 0 0.5rem;
        }
        
        .links a:hover {
            text-decoration: underline;
        }
        
        .divider {
            margin: 0 0.5rem;
            color: #9ca3af;
        }
    </style>
</head>
<body>
    <div class="signup-container">
        <div class="signup-header">
            <div class="icon-wrapper">
                <i class="fas fa-user-plus"></i>
            </div>
            <h2>Daftar Akun Siswa</h2>
            <p>Buat akun baru untuk mengakses Portal Siswa</p>
        </div>
        
        <div class="signup-body">
            <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                <div class="alert">
                    <i class="fas fa-exclamation-triangle"></i>
                    <% errors.forEach(error => { %>
                        <%= error %><br>
                    <% }); %>
                </div>
            <% } %>
            
            <% if (typeof success !== 'undefined' && success) { %>
                <div class="alert success-alert">
                    <i class="fas fa-check-circle"></i>
                    <%= success %>
                </div>
            <% } %>
            
            <form method="POST" action="/signup" style="width: 100%; display: flex; flex-direction: column; align-items: center;">
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-user"></i>
                        Nama Lengkap
                    </label>
                    <div class="input-group">
                        <i class="input-icon fas fa-user"></i>
                        <input type="text" class="form-control" name="name" placeholder="Masukkan nama lengkap Anda" 
                               value="<%= typeof formData !== 'undefined' ? formData.name || '' : '' %>" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-at"></i>
                        Username
                    </label>
                    <div class="input-group">
                        <i class="input-icon fas fa-at"></i>
                        <input type="text" class="form-control" name="username" placeholder="Masukkan username Anda" 
                               value="<%= typeof formData !== 'undefined' ? formData.username || '' : '' %>" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-envelope"></i>
                        Email
                    </label>
                    <div class="input-group">
                        <i class="input-icon fas fa-envelope"></i>
                        <input type="email" class="form-control" name="email" placeholder="Masukkan email Anda" 
                               value="<%= typeof formData !== 'undefined' ? formData.email || '' : '' %>" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-lock"></i>
                        Password
                    </label>
                    <div class="input-group">
                        <i class="input-icon fas fa-lock"></i>
                        <input type="password" class="form-control" name="password" placeholder="Minimal 6 karakter" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label class="form-label">
                        <i class="fas fa-lock"></i>
                        Konfirmasi Password
                    </label>
                    <div class="input-group">
                        <i class="input-icon fas fa-lock"></i>
                        <input type="password" class="form-control" name="confirmPassword" placeholder="Ulangi password Anda" required>
                    </div>
                </div>
                
                <button type="submit" class="btn-signup">
                    <i class="fas fa-user-plus"></i>
                    Daftar Sekarang
                </button>
            </form>
            
            <div class="links">
                <a href="/login">Sudah punya akun? Login</a>
                <span class="divider">|</span>
                <a href="/">Kembali ke Beranda</a>
            </div>
        </div>
    </div>
</body>
</html>