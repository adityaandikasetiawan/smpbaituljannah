<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <meta name="description" content="<%= news.excerpt || news.title %>">
  <meta name="keywords" content="<%= news.category || 'berita' %>, SMPIT Baituljannah, <%= news.title %>">
  <meta name="author" content="<%= news.author || 'SMPIT Baituljannah' %>">
  <meta name="robots" content="index, follow">
  <link rel="canonical" href="https://smpitbaituljannah.sch.id/berita/<%= news.id %>">
  
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/vendors.css">
  <link rel="stylesheet" href="/css/main.css">
  
  <!-- Favicon -->
  <link rel="icon" type="image/x-icon" href="/img/general/favicon.ico">
  <link rel="icon" type="image/png" sizes="16x16" href="/img/general/favicon-16x16.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/img/general/favicon-32x32.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/img/general/apple-touch-icon.png">
  
  <!-- Open Graph Meta Tags -->
  <meta property="og:title" content="<%= news.title %>">
  <meta property="og:description" content="<%= news.excerpt || news.title %>">
  <meta property="og:url" content="https://smpitbaituljannah.sch.id/berita/<%= news.id %>">
  <% if (news.featured_image) { %>
  <meta property="og:image" content="<%= news.featured_image %>">
  <% } %>
  <meta property="og:type" content="article">
  <meta property="og:site_name" content="SMPIT Baituljannah">
  <meta property="article:author" content="<%= news.author || 'SMPIT Baituljannah' %>">
  <meta property="article:published_time" content="<%= new Date(news.created_at).toISOString() %>">
  <% if (news.category) { %>
  <meta property="article:section" content="<%= news.category %>">
  <% } %>
  
  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image">
  <meta property="twitter:url" content="https://smpitbaituljannah.sch.id/berita/<%= news.id %>">
  <meta property="twitter:title" content="<%= news.title %>">
  <meta property="twitter:description" content="<%= news.excerpt || news.title %>">
  <% if (news.featured_image) { %>
  <meta property="twitter:image" content="<%= news.featured_image %>">
  <% } %>
  
  <!-- Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "NewsArticle",
    "headline": "<%= news.title %>",
    "description": "<%= news.excerpt || news.title %>",
    "author": {
      "@type": "Organization",
      "name": "<%= news.author || 'SMPIT Baituljannah' %>"
    },
    "publisher": {
      "@type": "Organization",
      "name": "SMPIT Baituljannah",
      "logo": {
        "@type": "ImageObject",
        "url": "https://smpitbaituljannah.sch.id/img/logo smp.png"
      }
    },
    "datePublished": "<%= new Date(news.created_at).toISOString() %>",
    "dateModified": "<%= new Date(news.updated_at || news.created_at).toISOString() %>",
    <% if (news.featured_image) { %>
    "image": "<%= news.featured_image %>",
    <% } %>
    "url": "https://smpitbaituljannah.sch.id/berita/<%= news.id %>",
    "mainEntityOfPage": {
      "@type": "WebPage",
      "@id": "https://smpitbaituljannah.sch.id/berita/<%= news.id %>"
    }
  }
  </script>
  
  <style>
    .news-detail-container {
      max-width: 800px;
      margin: 0 auto;
      padding: 40px 20px;
    }
    
    .news-detail-header {
      margin-bottom: 30px;
    }
    
    .news-detail-title {
      font-size: 2.5rem;
      font-weight: 700;
      line-height: 1.2;
      color: #1a1a1a;
      margin-bottom: 20px;
    }
    
    .news-detail-meta {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-bottom: 30px;
      padding-bottom: 20px;
      border-bottom: 1px solid #e5e5e5;
      font-size: 0.9rem;
      color: #666;
    }
    
    .news-detail-meta i {
      margin-right: 5px;
    }
    
    .news-detail-image {
      width: 100%;
      height: 400px;
      object-fit: cover;
      border-radius: 12px;
      margin-bottom: 30px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }
    
    .news-detail-content {
      font-size: 1.1rem;
      line-height: 1.8;
      color: #333;
    }
    
    .news-detail-content p {
      margin-bottom: 20px;
    }
    
    .news-detail-content h1,
    .news-detail-content h2,
    .news-detail-content h3,
    .news-detail-content h4,
    .news-detail-content h5,
    .news-detail-content h6 {
      margin-top: 30px;
      margin-bottom: 15px;
      font-weight: 600;
    }
    
    .back-to-news {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      color: #0066cc;
      text-decoration: none;
      font-weight: 500;
      margin-bottom: 30px;
      transition: color 0.3s ease;
    }
    
    .back-to-news:hover {
      color: #004499;
    }
    
    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 500;
      text-transform: uppercase;
    }
    
    .status-published {
      background-color: #d4edda;
      color: #155724;
    }
    
    @media (max-width: 768px) {
      .news-detail-title {
        font-size: 2rem;
      }
      
      .news-detail-meta {
        flex-direction: column;
        align-items: flex-start;
        gap: 10px;
      }
      
      .news-detail-image {
        height: 250px;
      }
      
      .news-detail-container {
        padding: 20px 15px;
      }
    }
  </style>
</head>
<body>
  <!-- Preloader -->
  <div class="preloader js-preloader">
    <div class="preloader__wrap">
      <div class="preloader__icon">
        <i class="icon icon-loading"></i>
      </div>
    </div>
  </div>
  
  <%- include('partials/header') %>
  
  <main class="main-content layout-pt-lg layout-pb-lg">
    <div class="container">
      <div class="news-detail-container">
        <!-- Back to News Link -->
        <a href="/berita" class="back-to-news">
          <i class="fas fa-arrow-left"></i>
          Kembali ke Daftar Berita
        </a>
        
        <!-- News Header -->
        <div class="news-detail-header">
          <h1 class="news-detail-title"><%= news.title %></h1>
          
          <div class="news-detail-meta">
            <div>
              <i class="fas fa-calendar-alt"></i>
              <%= new Date(news.published_at || news.created_at).toLocaleDateString('id-ID', { 
                year: 'numeric', 
                month: 'long', 
                day: 'numeric' 
              }) %>
            </div>
            
            <% if (news.author_full_name || news.author_name) { %>
            <div>
              <i class="fas fa-user"></i>
              <%= news.author_full_name || news.author_name %>
            </div>
            <% } %>
            
            <div>
              <i class="fas fa-tag"></i>
              <span class="badge bg-blue-1 text-white text-12 px-15 py-5 rounded-4">
                <%= news.category || 'Umum' %>
              </span>
            </div>
            
            <div>
              <span class="status-badge status-<%= news.status %>">
                <%= news.status === 'published' ? 'Dipublikasi' : news.status %>
              </span>
            </div>
          </div>
        </div>
        
        <!-- Featured Image -->
        <% if (news.featured_image) { %>
        <img src="<%= news.featured_image %>" alt="<%= news.title %>" class="news-detail-image">
        <% } %>
        
        <!-- News Excerpt -->
        <% if (news.excerpt) { %>
        <div class="news-detail-excerpt" style="font-size: 1.2rem; font-style: italic; color: #666; margin-bottom: 30px; padding: 20px; background-color: #f8f9fa; border-left: 4px solid #0066cc; border-radius: 4px;">
          <%= news.excerpt %>
        </div>
        <% } %>
        
        <!-- News Content -->
        <div class="news-detail-content">
          <%- news.content %>
        </div>
        
        <!-- Back to News Link (Bottom) -->
        <div style="margin-top: 50px; padding-top: 30px; border-top: 1px solid #e5e5e5; text-align: center;">
          <a href="/berita" class="button -blue-1 text-white">
            <i class="fas fa-arrow-left mr-10"></i>
            Kembali ke Daftar Berita
          </a>
        </div>
      </div>
    </div>
  </main>
  
  <%- include('partials/footer') %>
  
  <!-- Scripts -->
  <script src="/js/vendors.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>